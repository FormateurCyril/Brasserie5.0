{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row mt-2">
            {% for product in products %}
                {% include "product/_cardProduct.html.twig" with {'product' : product} only %}
            {% endfor %}
        </div>
    </div>
{% endblock %}